# Client Configuration
# API Server URL - This should point to your backend server
# For development: http://localhost:5000/api
# For production: https://your-api-domain.com/api
VITE_API_URL=http://localhost:5000/api

# Note: In Vite, all environment variables must be prefixed with VITE_
# These variables will be available in the browser via import.meta.env.VITE_API_URL
