/**
 * Notification Worker
 * Simplified version without Redis/BullMQ
 * Notifications are sent directly by the notification service
 */

let notificationWorker = null;

console.log("Notification worker not needed - notifications sent directly.");

module.exports = notificationWorker;
